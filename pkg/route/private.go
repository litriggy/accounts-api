package route

import (
	"github.com/gofiber/fiber/v2"
)

func PrivateRoutes(app *fiber.App) {
	//route := app.Group("/api/v1")

	//route.Get("/auth/check", middleware.CheckSession, controller.CheckSession)
}
